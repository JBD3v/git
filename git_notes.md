# Git basic commands

## Configure Git

```power_shell
git config --global user.name "Jerome.Bark"
git config --global user.email "jerome.bark@fastmail.com"
git config --list
```

## New repo

```power_shell
git init
git status
```
